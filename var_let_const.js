const browserVersion = 'Chrome'

function getBrowserVersion() {
if(browserVersion=='Chrome')
{
   let browserVersion = 'Edge';
   console.log('Browser version inside block is:',browserVersion);
   
}
console.log('Browser version outside block is:',browserVersion);
}
getBrowserVersion()

